# example-demo

## 运行
```
git clone git@github.com:JirafaYe/tiktok.git
cd example
cd user; go run cmd/user/main.go -addr="运行服务器ip"
cd hello; go run cmd/hello/main.go -addr="运行服务器ip"
cd gateway; go run main.go
```